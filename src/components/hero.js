import React from 'react';
import Logo from './logo';

/**
 * Displays a page-wide hero, just with the white logo.
 */
export default () => (
  <div>
    <div>
      <Logo size="40" isCentered/>
    </div>
  </div>
);
